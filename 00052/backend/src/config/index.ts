export { dbConfig, AppDataSource, default as database } from './database';
export {
  PORT,
  WS_PORT,
  JWT_SECRET,
  TEMPERATURE_UPDATE_INTERVAL,
  MIN_TEMP,
  MAX_TEMP,
  SCALE_3D,
} from './env';
